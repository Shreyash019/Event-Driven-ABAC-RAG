{
  "name": "arac-contracts-py",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": ["type:lib", "lang:python"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run pytest", "cwd": "packages/py" },
      "cache": true
    }
  }
}
